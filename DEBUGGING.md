# 故障排查

## 设置栏目未显示

确认：

1. `dsh-soul` 已安装到当前 Profile。
2. DSH 已完全重启。
3. 浏览器已刷新页面。

## 配置没有生效

保存配置后，需要发送一条新的消息。`agent.inject()` 面向下一次 Agent step，正在执行的模型请求不会被修改。

如果仍未生效，请确认当前运行的插件版本与安装版本一致，并重新安装或更新插件。

## 配置文件

配置文件位于：

```text
$DSH_HOME/soul-config.json
```

未设置 `DSH_HOME` 时，使用 DSH 默认用户数据目录。

## 日志

插件默认不输出运行日志。如需临时调试，可在源码中取消相关 `console` 语句的注释；调试完成后建议恢复注释。
